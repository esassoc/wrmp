# macOS
.DS_Store

# Editor / tool config
.vscode/

# Reference materials (large binaries, PDFs, Word docs)
reference/

# Playwright MCP temp files
.playwright-mcp/

# Reference archive
reference.zip

# Private project context (engagement framing, contacts, budgets, meeting history)
CLAUDE.local.md

# Local scratch: verification screenshots + throwaway dev server
/*.png
/serve.py

# Per-session agent scratch memory
.claude/agent-memory/

# Internal-facing deliverable working files — never publish to the public repo.
# The client-facing deliverables (1.4 recommendations page + assets) ARE tracked;
# these are the internal direction docs and the Word/markdown memo working files.
deliverables/*.docx
deliverables/~$*
deliverables/1.1-platform-evaluation-memo.md
deliverables/1.4-ecoatlas-integration-strategy.md
